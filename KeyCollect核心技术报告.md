# KeyCollect 核心技术报告

## 1. 项目概述

KeyCollect 是一个面向 RM65-6F 六自由度机械臂与 DexHand021 右手灵巧手的仿真遥操作、示教数据采集和模仿学习训练部署工程。当前主链路基于 MuJoCo 3.11.0 与 LeRobot 0.6.1，利用动作捕捉手套经 ROS1 发布的人手腕和手指数据，同步控制机械臂末端六维位姿及灵巧手 20 个关节，并将机器人状态、双视角 RGB、可选米制深度和控制动作记录为 LeRobot 数据集。

项目当前形成了以下闭环：

```text
Axis Studio / 动捕设备
        │ UDP
        ▼
mocap_joint_publisher（ROS1）
        │ /right_wrist_pose + /right_joint_poses
        ▼
MocapRosTeleop（rospy 或 ROSBridge）
        │ 6维末端增量 + 20维手指增量
        ▼
MuJoCoRobot（LeRobot Robot 插件）
        ├─ RM65 解析逆运动学 → 6个机械臂关节位置目标
        ├─ 五指增量积分/限幅 → 20个灵巧手关节位置目标
        ├─ MuJoCo 1 kHz 动力学与接触仿真
        └─ 状态、双RGB、可选深度观测
        │
        ├─ LeRobot 数据集采集、分批编号与合并
        └─ ACT 训练 → checkpoint → MuJoCo 在线闭环推理
```

需要准确界定项目现状：仓库中已完整接入训练与在线控制的是 ACT 模仿学习策略；仓库同时保存了 SmolVLA/SmolVLM 模型工件，但其特征定义来自另一套 22 维状态、20 维触觉和 22 维动作任务，与当前 KeyCollect 的 39 维状态、26 维动作接口并不一致，当前也没有对应的 KeyCollect SmolVLA 训练或推理入口。因此，SmolVLA 属于已有模型资产和后续 VLA 接入基础，不能表述为当前系统已经完成的 VLA 闭环部署。

## 2. 软件架构与模块化设计

项目通过两个 Python 包对 LeRobot 进行插件化扩展：

1. `lerobot_robot_mujoco` 注册 `mujoco` Robot 插件，负责场景加载、关节和执行器访问、状态采集、相机渲染、动作执行、场景重置与目标物随机化。
2. `lerobot_teleoperator_mocap_ros` 注册 `mocap_ros` Teleoperator 插件，负责 ROS 数据接入、标定、坐标映射、五指重定向、信号滤波和增量动作生成。

两者通过 Python entry point 注册到 LeRobot，而不是在 LeRobot 源码中硬编码。这种设计使标准 `lerobot-record` 能够根据配置中的 `robot.type: mujoco` 和 `teleop.type: mocap_ros` 自动发现本地实现，并保持数据集、训练器、预处理器及 checkpoint 格式与 LeRobot 生态兼容。

项目将参数集中在 `config/record_mujoco.yaml`：同一份配置同时描述仿真机器人、动捕遥操作器、相机、数据集和录制行为。独立遥操作入口则显式固化自己的标定参数，防止录制配置的变化无意影响日常调试控制。

## 3. 五指协同与人手—灵巧手重定向

### 3.1 灵巧手自由度组织

DexHand021 右手在当前 MuJoCo 场景中有 20 个可控转动关节，按拇指、食指、中指、无名指、小指排列，每指 4 个关节。场景共配置 26 个位置执行器，即机械臂 6 个加灵巧手 20 个。

当前“五指协同”不是神经网络或优化器学习出的协同空间，而是确定性的低维人体手部通道提取、解剖关节耦合和每关节并行控制。其主要机制如下。

### 3.2 57 维动捕信号的有效通道提取

手套发布的 `/right_joint_poses` 至少包含 57 个浮点数。代码从固定索引抽取拇指旋转、各指 MCP 屈曲和远端屈曲信息：

- 拇指使用索引 1、5、8，分别对应旋转、MCP 和远端屈曲；
- 食指使用 14，以及 17 与 20 的均值；
- 中指使用 26，以及 29 与 32 的均值；
- 无名指使用 38，以及 41 与 44 的均值；
- 小指使用 50，以及 53 与 56 的均值。

对两个远端测量通道取均值，可以抑制单点波动，并形成每根手指统一的远端弯曲量。

### 3.3 20 关节协同映射

人手通道相对第一帧中立姿态求差，再映射为 DexHand 20 维目标偏移：

- 拇指保留旋转、MCP、远端屈曲三个独立量，第 4 关节由远端屈曲乘 `pip_dip_coupling` 得到；
- 食指保留侧摆、MCP、远端屈曲，第 4 关节同样由远端屈曲耦合；
- 中指侧摆在当前映射中固定为 0；
- 无名指和小指侧摆由食指侧摆乘各自的 `finger_spread_coupling` 得到；
- 各指 PIP/DIP 类关节采用统一比例耦合，默认系数为 1.0。

需要注意，当前 `extract_glove_dofs()` 将 `index_spread` 明确设置为 0，因此默认实现下食指、无名指和小指的侧摆输出实际均为 0；配置中虽然保留了三项 spread coupling，但目前没有来自手套的有效食指侧摆信号驱动它们。这是报告中不应夸大的一点。

### 3.4 五指信号质量控制

动捕手指链路包含多层实时质量控制：

- 第一帧自动作为中立零点，也支持运行时重新归零；
- 输入要求至少 57 个有限数值，否则整帧拒绝；
- 相邻帧单通道变化超过 0.40 时，仅拒绝异常通道，同包内其他手指仍然更新；
- 对接受后的数据进行指数滤波，默认 `alpha=0.45`；
- 映射后的微小误差设置 0.003 rad 死区；
- 每控制帧手指动作限制为 0.05 rad；
- MuJoCoRobot 再次按照各关节物理范围裁剪目标。

这种“逐通道异常剔除 + 低通滤波 + 死区 + 速率限制 + 关节硬限位”的链路，主要解决动捕抖动、网络丢包、单通道跳变及灵巧手小惯量结构的瞬时冲击问题。

## 4. 基于动捕设备的机械臂遥操作

### 4.1 数据接入

外部动捕节点通过 ROS1 发布：

- `/right_wrist_pose`：`geometry_msgs/PoseStamped`，包含腕部三维位置和 xyzw 四元数；
- `/right_joint_poses`：`std_msgs/Float32MultiArray`，手指数据至少 57 维。

文档给出的典型发布频率约为 50 Hz。KeyCollect 支持两种接入方式：

- `rospy`：在兼容 ROS1 Python 环境时直接订阅；
- `rosbridge`：通过 `roslibpy` 连接 127.0.0.1:9090，解决 ROS Noetic 系统 Python 与 LeRobot Python 3.12 环境不一致的问题；
- `auto`：优先尝试 `rospy`，不可用时退回 ROSBridge。

订阅队列长度为 1，以减少历史消息堆积造成的控制延迟。回调和控制循环之间通过线程锁保护共享状态。

### 4.2 腕部位置标定与坐标映射

遥操作采用相对式控制。系统把收到的第一帧腕部位置记为中立位置，计算：

\[
\Delta p_r = S_p M_p(p_t-p_0)
\]

其中当前默认轴映射为机器人 XYZ 对应动捕 Z、X、Y，位置缩放系数为 0.01。系统维护已经下发的累计位置目标，并将目标误差转成逐帧增量；每轴每帧默认限制在 ±0.005 m。

### 4.3 腕部姿态映射

姿态使用四元数相对旋转，而不是直接对欧拉角做差。代码计算人体腕部本体坐标系增量：

\[
R_{rel}=R_0^T R_t
\]

再将其转换为旋转向量，通过标定轴矩阵映射到 RM65 局部旋转轴。默认标定关系为动捕 X/Y/Z 对应机器人局部 -Z/-X/+Y。映射后的旋转向量乘 0.4，意味着只使用手套腕部转角的 40%，再限制为每轴每帧 ±0.04 rad。

本体坐标系增量的使用避免了初始腕部偏航角改变后翻转、摆动和挥动轴互相串扰。四元数在输入时归一化，零四元数和非有限数据会被拒绝。

### 4.4 失联安全

腕部和手指数据分别记录最后更新时间。任一路数据超过 0.25 s 未更新时，该路输出安全零增量，而不是延续旧指令。腕部和手指就绪状态彼此独立，因此某一路短时失效不会伪造另一条链路的新动作。

## 5. RM65 运动学与连续控制

### 5.1 正逆运动学模型

RM65 模块采用 Craig 改进 D-H 约定：

\[
{}^{i-1}T_i=R_x(\alpha_{i-1})T_x(a_{i-1})R_z(q_i+offset_i)T_z(d_i)
\]

RM65-6F 参数为：

- `a=[0,0,0.256,0,0,0] m`；
- `alpha=[0,90,0,90,-90,90]°`；
- `d=[0.2405,0,0,0.210,0,0.1725] m`；
- `offset=[0,90,90,0,0,0]°`。

关节硬限位为 J1 ±178°、J2 ±130°、J3 ±135°、J4 ±178°、J5 ±128°、J6 ±360°。这些值同时出现在解析运动学和 MuJoCo 场景中，并有测试检查二者一致性。

### 5.2 封闭解析逆解

逆解首先沿法兰 z 轴回退 `d6` 求腕心，再解析求解肩部和肘部二连杆几何。常规位姿最多展开：

- 两个肩部分支；
- 两个肘部分支；
- 两个腕部分支。

每个候选角会扩展关节限位内相差 2π 的等价角，随后执行关节限位筛选、候选去重和完整 FK 回代。只有位置误差和姿态误差均在容差内的候选才会进入最终解集。腕部 `q5≈0` 时存在无穷多组 q4/q6 组合，代码使用当前 seed 的 q4 构造一组代表解。

### 5.3 连续分支选择

轨迹控制不是简单选择欧氏距离最近的单帧解。`RM65IKContinuitySelector` 对所有有效解析分支计算综合代价，包括：

- 相邻帧关节位移；
- 关节速度变化；
- 超过厂家最大关节速度的惩罚；
- 肩、肘、腕分支切换惩罚；
- 进入关节限位软区的惩罚；
- 腕部奇异附近 q4/q6 大幅补偿运动的惩罚。

第一帧以当前仿真关节角为 seed，后续帧保存上一解、上一速度和分支标签。肘部伸直或腕部 q5 接近零的奇异区域会降低相应分支切换惩罚，避免将数学上合并的分支误判为剧烈跳变。

### 5.4 笛卡尔增量执行

末端平移增量在世界坐标系积分，旋转增量在末端局部坐标系右乘积分。累积六维目标被变换到机械臂基坐标系后交给解析 IK，再转成 6 个关节位置目标。

当前代码还包含两级无解处理：

1. 将过远的法兰目标裁剪到预设安全球半径；
2. 完整姿态解析失败时，使用当前腕部姿态重试，以尽量保留平移控制；仍失败则恢复上一目标并保持当前机械臂命令。

该机制能避免无解时输出非法关节角，但其可达性裁剪是基于法兰点到基座的球形近似，不等价于带姿态和关节限位的真实六维可达空间。因此项目仍可能输出 `Analytic RM65 IK failed`。此外，独立入口 `scripts/teleop.py` 的安全半径为 0.5405 m，而 LeRobot Robot 插件实际导入的 `teleop_runtime.py` 使用 0.5785 m；两条入口目前并非完全相同的工作空间保护参数。这应作为现有技术边界和配置一致性问题记录，而不应描述为已经彻底解决。

### 5.5 数值运动学工具的实际状态

`rm65_kinematics.py` 还提供 SO(3) 指数/对数映射、MuJoCo 雅可比、阻尼最小二乘和雅可比条件数计算，用于测试及运动学辅助。但 README 明确说明当前主控制链已移除旧 DLS 控制，运行时机械臂使用解析 IK。配置中的 `ik_damping`、`ik_max_iterations`、`ik_pos_tol`、`ik_ori_tol` 是历史/预留配置，当前解析求解调用并不使用这些参数。

## 6. MuJoCo 仿真、接触与执行器建模

### 6.1 组合场景生成

`build_hardware_scene.py` 将 RM65-6F URDF 与 DexHand021 URDF 合并，并生成可直接运行的 MJCF。关键处理包括：

- 灵巧手通过固定变换安装到 `link_6`，默认安装偏移为 `[0,0,-0.08] m`；
- 添加机器人支架、工作台、地面、螺丝刀和相机；
- 为手掌建立 `dexhand_palm_center` site，用于相对手掌的目标物采样；
- 用凸六边形网格替换螺丝刀圆柱手柄，保证平面接触姿态稳定；
- 设置机器人子树重力补偿，任务物体仍受正常重力；
- 为灵巧手关节补充 URDF 导入时丢失的阻尼、转子惯量和摩擦损失；
- 使用 `implicitfast` 积分器、Newton 求解器、1 ms 步长和柔顺接触参数；
- 生成 26 个带位置、速度和力限幅的位置伺服执行器；
- 写入可重复恢复的 `home` keyframe。

机械臂各轴使用分级位置增益和力限幅，DexHand 使用统一 `kp=25`、`kv=2`、±0.5 力限幅。代码中的控制量是关节角位置目标，不是直接转矩。

### 6.2 多模态视觉观测

场景配置两个命名相机：

- `table_camera`：固定在 world，用于提供稳定的全局任务视角；
- `wrist_overhead_camera`：挂载在 `link_6`，随末端运动，用于观察近场手指闭合和物体接触。

当前录制配置可从 `table_camera` 同时生成 RGB 和米制深度。RGB 与深度来自同一个 MuJoCo 相机，因此在仿真中具有像素级对齐关系。深度通过 MuJoCo Renderer 的深度渲染模式直接取得，输出为 `float32`、形状 `(H,W,1)` 的米制距离，而不是将 RGB 灰度值当作深度。

### 6.3 任务域随机化

每个 episode 重置时可随机化螺丝刀位置和朝向。采样支持矩形工作区或扇环区域，并附加以下拒绝条件：

- 相对手掌前方/左右范围；
- 机械臂任务半径约束；
- 固定相机视锥范围及边缘裕量；
- 配置的世界坐标工作区。

螺丝刀朝向在相对世界 Y 轴的指定角度范围内随机。该机制用于增加物体位置与朝向多样性，同时避免生成明显不可见或不可操作的样本。当前没有光照、纹理、质量、摩擦或相机外参的随机化实现，不能将其概括为完整视觉域随机化。

## 7. 数据集采集与治理

### 7.1 统一的状态和动作接口

当前 KeyCollect 主数据接口为：

| 类型 | 组成 | 维度 |
|---|---|---:|
| `observation.state` | 6 机械臂位置 + 6 机械臂速度 + 20 手指位置 + 7 末端位姿 | 39 |
| `action` | 3 末端平移增量 + 3 末端旋转向量增量 + 20 手指关节增量 | 26 |
| 全局视觉 | `table_camera` RGB | 480×640×3 |
| 手腕视觉 | `wrist_overhead_camera` RGB | 480×640×3 |
| 可选深度 | `table_camera_depth`，米制 `float32` | 480×640×1 |

选择记录笛卡尔增量而不是解析 IK 后的 6 个关节目标，使示教动作保留“末端意图 + 五指动作”的统一语义。在线推理时，同一 26 维动作再次经过与采集相同的解析 IK、手指积分和安全裁剪，降低训练与部署控制接口不一致的风险。

### 7.2 录制控制与 episode 生命周期

项目包装 LeRobot 原生录制入口，只修改交互控制和 MuJoCo episode 重置行为，数据编码仍使用 LeRobot 实现：

- `q`、`n` 或右方向键：保存当前 episode 并进入下一条；
- `r` 或左方向键：丢弃并重录当前 episode；
- `Esc`：保存当前 episode 并终止会话；
- 每条正常结束的 episode 后恢复 `home` keyframe，并重新随机化螺丝刀；
- 跳过面向真机的手动复位倒计时，避免手套增量在仿真已重置后再次推动机器人。

数据支持后台流式视频编码。配置的两路 RGB 使用视频存储；深度由 LeRobot 深度编码路径单独处理，避免按普通 RGB 视频编码破坏物理量。

### 7.3 批次编号、并发保护与合并

`record_next.py` 在外部数据盘中选择第一个未占用的连续编号，使用文件锁和临时 reservation 文件避免多个启动器抢占同一 run。它区分：

- `depth`：`rm65_dexhand_depth_run_NNN`；
- `rgb`：`rm65_dexhand_run_NNN`。

`merge_datasets.py` 按 profile 收集并排序批次，通过 LeRobot `aggregate_datasets` 合并，支持显式排除质量不合格的 run。RGB 与 RGB+Depth 使用不同前缀，避免不同 feature schema 被误合并。

### 7.4 仓库内可核实的数据资产

仓库当前实际存在的 `data/rm65_dexhand_merged` 是双 RGB 数据集，而不是 RGB+Depth 数据集。其元数据为：

- 127 个 episode；
- 39,673 帧；
- 1 个任务；
- 30 FPS；
- 39 维状态；
- 26 维动作；
- 两路 480×640 RGB 视频。

当前 `config/record_mujoco.yaml` 已调整为 24 FPS、两路 RGB 加一路深度的新采集配置，但仓库中没有 `data/rm65_dexhand_depth_merged`。因此报告应区分“已存在的 30 FPS 双 RGB 数据资产”和“当前配置支持的 24 FPS RGB+Depth 新流程”。

## 8. ACT 训练与在线部署

### 8.1 已完成的双 RGB ACT 训练

仓库包含可核实的 40,000 步 ACT checkpoint：`outputs/train/act_rm65_dexhand/checkpoints/040000/pretrained_model`。其模型配置为：

- 输入：39 维状态、桌面 RGB、手腕 RGB；
- 输出：26 维增量动作；
- 视觉骨干：ImageNet 预训练 ResNet-18；
- Transformer：`dim_model=512`、8 个注意力头、4 层 encoder、1 层 decoder；
- 条件 VAE：启用，潜变量维度 32，KL 权重 10；
- 动作块长度 100，在线动作步数 100；
- 优化器：AdamW，学习率 `1e-5`，权重衰减 `1e-4`；
- batch size 32，训练 40,000 步，20,000 步保存一次；
- 图像、状态和动作使用 checkpoint 自带的预处理/归一化与反归一化工件；
- 训练配置没有环境在线评估，也未启用 W&B。

现有训练元数据没有成功率、真实抓取次数或独立测试集指标，因此不能在报告中给出未经记录的成功率。

### 8.2 RGB+Depth ACT 适配

标准 ACT ResNet-18 接收三通道图像，而项目深度保持单通道米制表示。`act_depth_adapter.py` 在 ACT backbone 前注册 pre-hook：仅当输入为 BCHW 且通道数为 1 时，将同一深度值复制到 3 个通道；RGB 输入保持原样。

`train_act_depth.py` 强制 `dataset.depth_output_unit=m`，保证数据解码、训练统计和在线 MuJoCo 推理都使用米。适配器是幂等安装的，不会重复注册 hook。该方案复用共享 ResNet 结构，没有引入独立深度编码器或显式 RGB-D 融合模块。

仓库已提供 RGB+Depth 训练命令和前向兼容测试，但尚无本地合并深度数据集或深度 ACT checkpoint。因此应表述为“训练和部署代码路径已经实现并测试”，而不是“已经完成深度模型训练”。

### 8.3 在线推理闭环

`infer_mujoco.py` 的在线流程为：

1. 自动定位 checkpoint 的 `pretrained_model`；
2. 从数据集元数据恢复特征形状，从 checkpoint 恢复策略配置；
3. 按 CUDA 可用性选择设备，显式请求 CUDA 而不可用时直接失败；
4. 使用与训练一致的 preprocessor 和 postprocessor；
5. 从 MuJoCo 获取 39 维状态和 checkpoint 声明的相机；
6. 将 HWC 图像转为 CHW，RGB uint8 缩放到 `[0,1]`，米制深度保持浮点值；
7. 调用 `policy.select_action()`，ACT 可缓存动作块，避免每个控制周期都重新前向；
8. 将 26 维策略输出还原为 6 维末端增量和 20 维手指增量；
9. 通过与示教采集相同的 `MuJoCoRobot.send_action()` 执行；
10. 可选保存推理视频，并在退出时释放仿真和视频资源。

推理前会检查 checkpoint 需要的相机是否存在，避免用缺失观测静默运行。需要注意，脚本从命令行指定的数据集根目录读取归一化和特征元数据，因此部署时必须保持 checkpoint、数据集 schema、相机名称和机器人动作维度一致。

## 9. VLA/SmolVLA 工件及其与当前系统的关系

仓库保存了：

- `model/SmolVLM2-500M-Video-Instruct` 视觉语言基础模型；
- `model/smolvla_base` SmolVLA checkpoint、预处理器、后处理器和训练清单。

该 SmolVLA 配置使用：

- 两路 256×256 RGB：`observation.image` 和 `observation.wrist_image`；
- 22 维 `observation.state`；
- 20 维 `observation.tactile`；
- 22 维动作；
- 动作 chunk 20、每次执行 10 步；
- SmolVLM2-500M 视觉语言骨干；
- 训练时采用状态投影、动作专家和指尖触觉 MLP 残差融合。

训练清单记录的是另一目录下的苹果抓取/触觉数据与自定义训练脚本，不在当前 KeyCollect 源码树内。其选择文件记录 step 3000 的验证损失约 0.05273，但该值不能直接代表 KeyCollect 螺丝刀任务性能。

当前 KeyCollect 与该 SmolVLA 工件存在四项接口差异：状态 39 对 22、动作 26 对 22、当前无触觉 observation、相机 key 和分辨率不同。因此，现有 `infer_mujoco.py` 虽通过 LeRobot factory 加载策略，但其动作还原逻辑固定要求 26 维，不能直接部署该 SmolVLA 工件。要形成真正的 KeyCollect VLA 闭环，需要重新定义统一 schema、补充语言任务输入、决定是否接入触觉、编写 KeyCollect SmolVLA 训练配置，并使推理动作适配器按 checkpoint schema 工作。

## 10. 安全、可靠性与验证体系

### 10.1 多层安全机制

系统的安全措施覆盖输入、规划、执行和仿真四层：

- 输入层：消息维度和有限性检查、四元数归一化、超时归零、手指离群通道拒绝；
- 指令层：末端平移、旋转和手指逐帧限幅，手指死区和滤波；
- 运动学层：解析分支全枚举、FK 回代、关节硬限位、软限位代价、速度和奇异性惩罚；
- 执行层：关节范围裁剪、NaN/Inf 检查、位置伺服力限幅；
- 仿真层：隐式积分、接触柔顺性、手指阻尼/惯量/摩擦、机器人重力补偿；
- 数据层：episode 重录、批次隔离、编号并发保护和 schema 分流。

### 10.2 自动化测试覆盖

仓库测试覆盖以下关键事实：

- RM65 解析全解 FK 回代、seed 最近支路、不可达异常、腕部奇异和连续选择代价；
- 官方 MDH 与 MuJoCo 随机关节角 FK 一致性；
- 关节限位一致性、TCP 偏移和腕部局部轴控制；
- 腕部坐标映射、四元数相对姿态、57 维手指索引及 PIP/DIP 耦合；
- 中立帧、逐帧限幅和动捕消息超时安全；
- 26 个位置伺服、home 姿态保持、固定/腕部相机挂载；
- 米制深度形状和值域；
- LeRobot 插件发现；
- ACT 单通道深度适配、幂等安装和模型前向。

本次代码核查实际运行了 `rm65/test_rm65_ik.py` 的 6 项测试，全部通过。其余测试在仓库中存在，但本报告不将“存在测试代码”扩大表述为本次环境已完整执行通过。

## 11. 可作为报告重点提炼的核心技术

综合代码实现，KeyCollect 最有依据的核心技术可概括为：

1. **异构 ROS—LeRobot 动捕接入技术**：以 rospy/ROSBridge 双路径解决 ROS1 与现代 Python 训练环境兼容问题，并通过插件接口接入标准数据流水线。
2. **机械臂—五指统一增量动作空间**：将腕部六维运动和 20 关节灵巧手动作组织为 26 维统一策略输出，使人类示教与策略部署共用同一执行接口。
3. **基于人体解剖耦合的五指重定向**：从 57 维手套数据选择有效关节通道，通过远端均值、PIP/DIP 耦合、跨指侧摆关系和多级滤波生成稳定的 20 关节动作。
4. **RM65 封闭解析全解与轨迹连续性选择**：枚举肩/肘/腕分支，结合 FK 验证、限位、速度、分支滞回和奇异区代价实现连续笛卡尔遥操作。
5. **面向抓取接触的高保真仿真场景构建**：自动合并机械臂与灵巧手模型，补偿 URDF 导入缺失的动力学参数，设计位置伺服、力限幅、柔顺接触和稳定目标物几何。
6. **多视角 RGB 与像素对齐米制深度采集**：固定全局视角和腕部局部视角协同，支持同相机 RGB-D 对齐，并保持深度单位贯穿采集、训练和推理。
7. **可治理的 LeRobot 示教数据生产线**：支持 episode 级保存/重录、自动复位、任务随机化、流式编码、并发安全编号、RGB/RGB-D schema 隔离和批量合并。
8. **ACT 端到端模仿学习闭环**：已有双 RGB 40,000 步模型工件，训练和推理共享状态、动作、相机、归一化与机器人执行链；另已实现单通道米制深度适配路径。

## 12. 当前技术边界与后续工作

为了保持报告可信度，以下内容应明确列为现状边界或后续工作：

- 当前系统控制和数据采集对象是 MuJoCo 仿真，不是 RM65/DexHand 真机驱动；
- 当前主要任务是抓取螺丝刀，数据元数据只有一个任务标签；
- 现有合并数据集为 30 FPS 双 RGB，而当前配置已经转向 24 FPS RGB+Depth，二者不能混写；
- 已完成的在线策略链是 ACT，不是语言条件 VLA；
- SmolVLA 工件和当前 KeyCollect schema 不兼容，尚未形成当前项目的训练部署闭环；
- 当前 KeyCollect 状态中没有触觉输入，不能声称具有触觉闭环抓取；
- 五指侧摆映射配置虽存在，但当前有效输入被固定为零；
- 解析 IK 的球形可达性保护不能完整表达带姿态和限位的六维工作空间；
- 当前无碰撞约束 IK、自碰撞规划或环境避障规划；
- 当前数据随机化集中在螺丝刀位置和朝向，尚无完整视觉/动力学域随机化；
- 仓库没有记录 ACT 的独立测试成功率，也没有当前 SmolVLA 在螺丝刀任务上的评测结果；
- RM65 `d6` 在运动学说明中由工作半径推导，真机部署前仍需以具体型号原始参数与示教器位姿进行复核。
- 独立遥操作与 LeRobot 录制/推理链的 RM65 安全半径分别为 0.5405 m 和 0.5785 m，需要统一后才能声称控制边界完全一致；
- 场景生成脚本当前定义的 RM65 home 为 `[0,-23,-99,0,27,180]°`，已生成 MJCF 也采用该姿态，但 `rm65_kinematics.py` 和场景集成测试仍以 `[0,-90,45,0,-45,180]°` 为参考；
- 场景生成脚本定义的固定相机为 `(-0.50,0,0.80)`、145° FOV，而当前已生成 MJCF 使用约 `(-0.6502,0.1883,1.0314)`、70° FOV。当前运行以 MJCF 为准，但重新生成场景可能改变数据分布；
- 上述 home、相机和安全半径差异说明生成源、运行工件和测试基线存在版本漂移，应在正式验收前统一并重新运行全量测试。

## 13. 代码依据索引

- 项目总览与操作流程：`README.md`
- 统一录制配置：`config/record_mujoco.yaml`
- 动捕遥操作器：`lerobot_teleoperator_mocap_ros/lerobot_teleoperator_mocap_ros/mocap_ros.py`
- 手腕/五指重定向：`lerobot_teleoperator_mocap_ros/lerobot_teleoperator_mocap_ros/retargeting.py`
- RM65 解析运动学：`rm65/rm65_ik.py`
- MuJoCo LeRobot 插件：`lerobot_robot_mujoco/lerobot_robot_mujoco/mujoco.py`
- 仿真封装和深度渲染：`lerobot_robot_mujoco/lerobot_robot_mujoco/simulation.py`
- 场景构建：`scripts/build_hardware_scene.py`
- episode 录制控制：`scripts/record_mujoco.py`
- 批次编号与并发保护：`scripts/record_next.py`
- 数据集合并：`scripts/merge_datasets.py`
- ACT 深度训练入口：`scripts/train_act_depth.py`
- ACT 深度适配：`scripts/act_depth_adapter.py`
- ACT 在线推理：`scripts/infer_mujoco.py`
- 实际双 RGB 数据元数据：`data/rm65_dexhand_merged/meta/info.json`
- 实际 ACT 配置：`outputs/train/act_rm65_dexhand/checkpoints/040000/pretrained_model/config.json`
- SmolVLA 工件配置：`model/smolvla_base/config.json`
- SmolVLA 训练来源记录：`model/smolvla_base/training_manifest.json`
