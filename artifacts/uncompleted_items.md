# 未完成事项

以下项目不能仅通过修改当前仓库得到真实结果：

- 受管 `sensor_msgs/JointState` 接收协议已实现，但动捕发布节点不在本仓库；源端频率、源时间戳和帧序号仍需升级发布端后实测。旧 `Float32MultiArray` 兼容链没有 Header。
- 15 个已消费通道现在可按 `JointState.name` 严格校验；四个手指侧摆没有已标定真实源通道，仍固定为 0。
- 仿真已区分停止更新、保持、受控减速和锁存急停；真机执行器响应和物理安全回路仍需硬件验证。
- `d6=0.1725 m` 已由本地 `assets/arm/RM65-6F.urdf` 及其 SHA-256 固化，但厂家原始主来源和示教器 FK 对照仍待归档。
- 历史 127 条 episode 的成功、失败原因、物体初始位姿、当时场景、批次、随机种子和四阶段时间戳没有被记录，无法追溯补造。
- 现有 40k checkpoint 缺少训练/验证损失历史、硬件、耗时和当时依赖快照，且使用了全部 127 条轨迹。新训练入口已保存这些信息，但必须实际重训。
- RGB-D 仅完成米制深度路径和三通道输入适配；独立数据集、checkpoint、双 RGB 对照实验及深层特征融合均待实施。
- 40 维方向触觉 schema、独立数据 profile 和触觉阈值约束已有代码；力阈值、滤波参数及张开方向仍需标定。
- 完整导纳控制没有启用或声称实现；若开发，仍需指尖 FK/雅可比/IK、冗余约束、接触切换和稳定性验证。
- 整段轨迹及附着工具碰撞检查已实现；简化手模的指间自碰几何仍需清理，也没有全局避障路径规划。
- 在线目标丢失抢占仍需经验证的视觉检测器。
- 100 个固定 case 和自动评测执行器已实现，但只运行了不产生成功率的单步冒烟；完整成功率、力/滑移指标和延迟分布待新模型实测。
